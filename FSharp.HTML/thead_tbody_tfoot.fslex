%{
open FslexFsyacc.Runtime
type token = string
%}

%%
// NOTE: every line Matching cannot be nullable
// [td th ] ? tr ? [ thead tbody tfoot ] ? {} It should be divided into three non-nullable patterns:
[td th ]   tr ? [ thead tbody tfoot ] ? {lexbuf}
[td th ] ? tr   [ thead tbody tfoot ] ? {lexbuf}
[td th ] ? tr ? [ thead tbody tfoot ]   {lexbuf}
[caption colgroup] {lexbuf}
"*"     { [] }
